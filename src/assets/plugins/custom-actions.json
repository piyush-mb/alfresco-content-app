{
  "$schema": "../../../extension.schema.json",
  "$version": "1.0.0",
  "$name": "plugin1",
  "$description": "demo plugin",

  "routes": [
    {
      "id": "custom.actions.route",
      "path": "ext/my/route",
      "component": "custom-actions.main.component"
    }
  ],

  "features": {
    "navbar": [
      {
        "id": "custom.actions.nav",
        "items": [
          {
            "id": "custom.actions.main",
            "icon": "extension",
            "title": "Custom Actions",
            "route": "custom.actions.route"
          }
        ]
      }
    ]
  }
}
